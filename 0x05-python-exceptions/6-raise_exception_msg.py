#!/usr/bin/python3
def raise_exception_msg(message=""):
    """
    raises exception with message

    Args:
        message (str, optional): the exception message. Defaults to "".
    """
    raise NameError(message)
